#include "main.h"
/**
 * _isalpha - Checks if a character is alphabet.
 * @c: The character to be checked.
 *
 * Return: 1 if character is lettre, 0 otherwise.
 */
int _isalpha(int c)
{
if (isalpha(c))
{
return (1);
}
else
return (0);
}
