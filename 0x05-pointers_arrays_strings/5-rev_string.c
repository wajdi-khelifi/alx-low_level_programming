#include "main.h"
/**
* rev_string - reverses a string.
* @s: pointer to the string to point
* Return: void
*/
void rev_string(char *s)
{
	char swap;
	int i = 0;

	while (s[i])
	{
		i++;
	}
	while (i--)
	{
		swap = s[i];
	}
	return (swap);
}
